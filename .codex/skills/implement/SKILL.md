---
name: implement
description: ブランチ作成から検証とコミットまで含めて通常のコード実装を行うときに使う。
---

# Implement

以下の手順で、コード実装作業を行う。

## 目的

ブランチ作成、タスク記録、コード実装、検証、`git add`、コミットまでを含む通常の実装作業を行う。

## 前提

作業前に `$start-work` を使い、作業ブランチを作成する。

## 実装ルール

- 既存の構成、命名、実装パターンを優先する。

## 検証

プロジェクトに設定されている検証コマンドを確認し、必要に応じて実行する。

```bash
git status --short --branch
```

ビルド、型チェック、lint、テストが設定されている場合は実行し、実行できない場合は理由を記録する。

## 完了手順

変更内容を確認し、適切な単位でステージしてコミットする。

```bash
git diff
git add <changed-files>
git commit -m "<変更内容がわかる具体的な日本語メッセージ>"
```

`git push` などのリモート操作は、ユーザーが明示的に許可または依頼した場合のみ行う。
