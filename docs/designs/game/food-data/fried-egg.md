# fried-egg.ts

## 責務

目玉焼きの個別食品定義を公開する。

## データ仕様

`friedEggFoodInfo`: id/spriteId=`fried-egg`、name=`目玉焼き`、ingredientIds=`['egg']`、process=`heating`、storage生成不可、提供可、材料利用可、difficulty=1。`as const satisfies FoodInfoData`。

## 関数仕様

関数は定義しない。
