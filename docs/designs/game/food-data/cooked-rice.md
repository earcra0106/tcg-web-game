# cooked-rice.ts

## 責務

ごはんの個別食品定義を公開する。

## データ仕様

`cookedRiceFoodInfo`: id/spriteId=`cooked-rice`、name=`ごはん`、ingredientIds=`['rice']`、process=`heating`、storage生成不可、提供可、材料利用可、difficulty=1。`as const satisfies FoodInfoData`。

## 関数仕様

関数は定義しない。
